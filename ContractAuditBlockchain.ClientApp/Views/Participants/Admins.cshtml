@model IEnumerable<AdminParticipantListViewModel>
@{
    ViewBag.Title = "Admin Users";
}

@Html.Partial("_CreateEditAdmin", new AdminParticipantCreateEditViewModel())

<div class="col-md-12 text-center">
    <h2>@(ViewBag.Title)</h2>
    <button type="button" class="btn btn-info pull-right" data-toggle="modal" data-target="#CreateAdminModal"><span class="glyphicon glyphicon-plus"></span> New Admin</button>
</div>


<div class="row col-xs-12">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var aParticipant in Model)
            {
                <tr>
                    <td>@(aParticipant.ID)</td>
                    <td>@(aParticipant.Name)</td>
                    <td>
                        <button type="button" class="btn btn-info btn-sm detailsAdminParticipantButton" data-participantid="@(aParticipant.ID)"><span class="glyphicon glyphicon-list-alt"></span> Details</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>

<div class="clearfix"></div>

@section scripts {
    <script>
        $(document).ready(function () {
            $('.detailsAdminParticipantButton').click(function (e) {
                var participantId = $(e.currentTarget).data('participantid');
                if (participantId) {
                    window.location = '@(Url.Action("DetailsForAdmin", "Participants"))/' + participantId;
                }
            });

        });
    </script>
}
